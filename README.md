# MedCare - Medical Consultation Platform

<div align="center">
  <img src="https://cdn-icons-png.flaticon.com/512/2966/2966327.png" alt="MedCare Logo" width="120" />
  <h3>A Modern Platform for Medical Consultations</h3>
</div>

## 📋 Обзор

MedCare - это веб-платформа для онлайн-консультаций с врачами. Платформа поддерживает различные методы аутентификации, включая традиционный email/пароль и Google OAuth. Она включает ролевую систему доступа (пациент, врач, администратор), управление профилями врачей, систему заявок для верификации врачей и сервисы, основанные на местоположении.

## ✨ Функциональность

- **Аутентификация**
  - Традиционный вход по email/паролю с верификацией email
  - Интеграция с Google OAuth
  - Управление профилями пользователей

- **Роли пользователей**
  - Пациент: поиск врачей и запрос консультаций
  - Врач: проведение консультаций и управление профессиональным профилем
  - Администратор: управление пользователями, верификация заявок врачей и мониторинг активности платформы

- **Система верификации врачей**
  - Процесс подачи заявок для медицинских специалистов
  - Загрузка документов (дипломы, лицензии)
  - Процесс рассмотрения и одобрения администратором
  - Система уведомлений о статусе

- **Геолокационные сервисы**
  - Фильтрация врачей по районам
  - Предпочтения по местоположению для пациентов

## 🛠️ Технологический стек

### Бэкенд
- FastAPI (Python веб-фреймворк)
- SQLAlchemy (ORM)
- JWT для аутентификации
- Pydantic для валидации данных
- Alembic для миграций базы данных

### Фронтенд
- React.js с Vite
- NextUI для UI компонентов
- Tailwind CSS для стилей
- React Router для навигации
- Zustand для управления состоянием
- Интеграция с Google OAuth

## 🚀 Начало работы

### Предварительные требования
- Python 3.10+ 
- Node.js 14+
- npm или yarn
- MySQL или PostgreSQL база данных

### Настройка

#### Клонирование репозитория
    ```bash
git clone https://github.com/yourusername/medcare.git
cd medcare
```

#### Настройка бэкенда
1. Создайте и активируйте виртуальное окружение:
    ```bash
cd backend
    python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
    ```

2. Установите зависимости:
    ```bash
    pip install -r requirements.txt
    ```

3. Создайте файл `.env` в директории `backend`:
```
SECRET_KEY=your_secret_key_here
DATABASE_URL=mysql+pymysql://username:password@localhost/medcare
GOOGLE_CLIENT_ID=735617581412-e8ceb269bj7qqrv9sl066q63g5dr5sne.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=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
GOOGLE_REDIRECT_URI=http://localhost:5173/auth/google/callback
VERIFICATION_BASE_URL=http://localhost:5173/verify-email
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USERNAME=your_email@gmail.com
EMAIL_PASSWORD=your_app_password
EMAIL_FROM=your_email@gmail.com
APP_ENV=development
```

4. Запустите миграции базы данных:
    ```bash
    alembic upgrade head
    ```

5. Запустите сервер бэкенда:
    ```bash
    uvicorn main:app --reload
    ```

#### Настройка фронтенда
1. Установите зависимости:
    ```bash
cd frontend
    npm install
    ```

2. Создайте файл `.env` в директории `frontend`:
```
VITE_API_URL=http://localhost:8000
VITE_GOOGLE_CLIENT_ID=735617581412-e8ceb269bj7qqrv9sl066q63g5dr5sne.apps.googleusercontent.com
```

3. Запустите сервер разработки:
    ```bash
    npm run dev
    ```

4. Откройте http://localhost:5173 в браузере

## 🔑 Настройка Google OAuth

Платформа использует Google OAuth для аутентификации. Следуйте этим шагам для настройки:

1. Создайте проект в [Google Cloud Console](https://console.cloud.google.com/)
2. Включите экран согласия OAuth и настройте его:
   - Установите имя приложения, контактный email и контактную информацию разработчика
   - Добавьте области доступа: `./../auth/userinfo.email` и `./../auth/userinfo.profile`

3. Создайте учетные данные OAuth:
   - Установите тип приложения как "Веб-приложение"
   - Добавьте разрешенные JavaScript источники: `http://localhost:5173`
   - Добавьте разрешенные URI перенаправления: `http://localhost:5173/auth/google/callback`

4. Обновите учетные данные в файлах `.env` для бэкенда и фронтенда, как показано выше

## 📁 Структура проекта

```
medcare/
├── backend/
│   ├── alembic/            # Миграции базы данных
│   ├── uploads/            # Хранилище загруженных файлов
│   ├── auth.py             # Логика аутентификации
│   ├── main.py             # FastAPI приложение, API эндпоинты
│   ├── models.py           # Модели базы данных
│   ├── schemas.py          # Pydantic схемы
│   ├── requirements.txt    # Python зависимости
│   └── .env                # Переменные окружения
├── frontend/
│   ├── public/             # Статические файлы
│   ├── src/
│   │   ├── api/            # API клиенты
│   │   ├── assets/         # Изображения, шрифты и т.д.
│   │   ├── components/     # React компоненты
│   │   ├── pages/          # Компоненты страниц
│   │   ├── stores/         # Управление состоянием (Zustand)
│   │   ├── App.jsx         # Основной компонент приложения
│   │   └── main.jsx        # Точка входа
│   ├── package.json        # JavaScript зависимости
│   └── .env                # Переменные окружения
└── README.md               # Этот файл
```

## 🔄 Процесс аутентификации

### Традиционный вход
1. Пользователь регистрируется с email/паролем
2. Отправляется письмо для верификации (симулируется в режиме разработки)
3. Пользователь подтверждает email по ссылке верификации
4. Пользователь входит с email/паролем
5. Бэкенд выдает JWT токен

### Вход через Google OAuth
1. Пользователь нажимает "Войти через Google"
2. Пользователь перенаправляется на аутентификацию Google
3. После успешной аутентификации Google перенаправляет обратно с кодом авторизации
4. Фронтенд отправляет код авторизации на бэкенд
5. Бэкенд проверяет код с Google API
6. Если проверка прошла успешно, бэкенд создает/обновляет пользователя и выдает JWT токен
7. Если пользователь входит в первый раз, ему предлагается заполнить профиль

## 🧑‍💻 Рекомендации по разработке

### Бэкенд
- Используйте Pydantic модели для валидации данных
- Следуйте паттернам инжекции зависимостей FastAPI
- Пишите docstrings для всех функций
- Используйте alembic для изменений схемы базы данных

### Фронтенд
- Используйте функциональные компоненты с хуками
- Поддерживайте согласованную структуру компонентов
- Используйте API клиент для всей коммуникации с бэкендом
- Следуйте соглашениям о наименовании проекта

## 🔧 Исправленные ошибки

В процессе работы над проектом были исправлены следующие проблемы:

1. **Бэкенд**:
   - Исправлена ошибка форматирования в функции `send_verification_email`, которая вызывала неожиданный отступ и ошибку синтаксиса в main.py
   - Обновлена версия bcrypt с 4.3.0 до 4.0.1, чтобы исправить ошибку "error reading bcrypt version" и несовместимость с passlib
   - Добавлена корректная обработка ошибок в эндпоинте `/notifications` для случаев, когда данные отсутствуют или имеют неправильный формат

2. **Фронтенд**:
   - Исправлена функция `fetchApplicationUpdates` в компоненте `Notification.jsx` для проверки, является ли полученный `data` массивом перед использованием метода filter
   - Предотвращен автоматический сброс данных при каждой загрузке компонента `ApplicationStatusTracker`, который приводил к постоянным запросам на сервер
   - Обновлены компоненты `Button` в `HomePage.jsx` для использования `onPress` вместо `onClick`, что устраняет предупреждения NextUI
   - Сохранены нативные HTML кнопки в `AuthPage.jsx` с их обработчиками событий `onClick`
   - Добавлены toast-уведомления вместо алертов в формах профилей врача и пациента для более современного пользовательского интерфейса
   - Добавлен ToastContainer в main.jsx для корректного отображения уведомлений
   - Упрощен интерфейс страницы истории путем удаления неиспользуемой вкладки "Платежи"

3. **Документация**:
   - Создан подробный README.md на русском и английском языках с инструкциями по настройке и запуску
   - Добавлены примеры конфигурационных файлов .env для бэкенда и фронтенда
   - Описаны процессы аутентификации, включая интеграцию Google OAuth
   - Обновлена документация по последним изменениям интерфейса и функциональности

## 🤝 Вклад в разработку

Мы приветствуем ваш вклад! Пожалуйста, не стесняйтесь создавать Pull Request.

## 📝 Лицензия

Этот проект лицензирован под MIT License - смотрите файл LICENSE для получения подробной информации.